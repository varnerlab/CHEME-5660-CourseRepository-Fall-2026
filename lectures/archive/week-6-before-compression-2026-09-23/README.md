# Week 6 before the September 23 compression

This verified snapshot preserves the original SIM-estimation L6a and SIM-portfolio L6b before combining them into the new L6b. The new L6a restores data-driven minimum variance from the September 18 Week 5 archive. Both Week 7 meetings are reserved for utility-based alternatives.

The `week-6/` payload is byte-for-byte unchanged, including notebooks, saved outputs, data, figures, slide sources and PDFs. `SHA256SUMS` and `inventory.json` record all payload files. The root Julia environment is saved under `environment/`; package code and market data are versioned under the source commit recorded in the inventory. Restore the original `lectures/week-6/` layout in a separate checkout for its relative dependencies. Do not rewrite this archival payload when changing the active week.
