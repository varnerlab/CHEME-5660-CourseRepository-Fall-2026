# Optional advanced Markov/HMM module

This folder preserves the former Markov-model lecture and its examples as an
optional L12a extension. Run the notebooks from this directory so that their
local `Include.jl`, `data/`, and figure paths resolve together.
