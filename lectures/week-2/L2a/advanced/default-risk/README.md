# Optional advanced default-risk example

This notebook extends the deterministic cash flows in L2a to uncertain
payments. Run it from this directory; the local `Include.jl` activates the
course environment and loads the packages used by the analytical and Monte
Carlo calculations.
